{
  "name": "@blockcore/nostr-login-workspace",
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "dependencies": {
    "@nostr-dev-kit/ndk": "^2.4.0"
  },
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
